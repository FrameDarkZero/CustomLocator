# CustomLocator
CustomLocator is a Maya Python tool that replaces default locators with custom, color-coded axis locators for clear joint placement. It supports side-aware naming, orientation control, and robust error handling. This tool is the foundation of a future Qt-based auto-rig system.
